<%=link_to 'New Land', new_taxpayer_land_path(@taxpayer), class: 'btn btn-primary' %>

<%=link_to 'New Building', new_taxpayer_building_path(@taxpayer), class: 'btn btn-primary' %>
<%=link_to 'New Machinery', new_taxpayer_machinery_path(@taxpayer), class: 'btn btn-primary' %>
